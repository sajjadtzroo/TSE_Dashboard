# Core scraping framework
# Pinned >=2.14.2 to fix GHSA-cwxj-rr6w-m6w7 (Referrer-Policy header RCE
# in <2.14.2: malicious target site can force arbitrary Python import).
scrapy>=2.14.2,<3

# Database ORM and migrations
sqlalchemy>=2.0.0
alembic>=1.13.0

# Job scheduling
apscheduler>=3.10.0

# Configuration and utilities
# Pinned >=1.2.2 to fix CVE-2026-28684 (set_key/unset_key follow symlinks
# and can overwrite arbitrary writable files via a pre-placed .env symlink).
python-dotenv>=1.2.2
pydantic>=2.9.0
pytz>=2023.3

# PostgreSQL support
psycopg2-binary>=2.9.9

# RSS/news feeds
feedparser>=6.0

# HTTP requests
requests>=2.31.0
# Pinned >=3.10.11 to fix CVE-2024-23334 (static-route path traversal,
# actively exploited) and CVE-2024-52304 (request smuggling).
aiohttp>=3.10.11
aiohttp-socks>=0.8.0        # SOCKS5 proxy support for aiohttp

# Metrics
prometheus-client>=0.20.0   # standalone Prometheus metrics server (tick_ingestor)

# Persian/Jalali date support
jdatetime>=5.0.0

# Data validation and parsing
python-dateutil>=2.8.2

# PDF/RAG pipeline
PyMuPDF>=1.24.0
pytesseract>=0.3.10
pdf2image>=1.17.0
# Pinned >=12.2.0 to fix Pillow PSD/PDF DoS + memory-corruption cluster
# (GHSA-r73j-pqj5-w3x7, GHSA-pwv6-vv43-88gr, etc.) — directly in our PDF
# rasterisation path.
pillow>=12.2.0
langchain-text-splitters>=0.3.0
openai>=1.0.0
tavily-python>=0.3.0
websockets>=12.0
pgvector>=0.3.0
numpy>=1.24.3
tqdm>=4.66.1

# Object storage
minio>=7.0.0

# Cross-encoder reranking (optional, set RERANKER_ENABLED=true to activate)
sentence-transformers>=2.2.0

# Telegram real-time ingestion
telethon>=1.36.0
aioredis>=2.0.0

# Load / performance testing (not installed in Docker — run on host)
locust>=2.24.0

# Excel export
openpyxl>=3.1.2
numpy-financial>=1.0.0

# Commodity market data (Yahoo Finance)
yfinance>=0.2.36
